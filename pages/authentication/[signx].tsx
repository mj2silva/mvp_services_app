import { FC } from 'react';
// import { useRouter } from 'next/router';

const Signx:FC = () => (
  // const router = useRouter();
  // const { signx } = router.query;
  // mandar accounts Form
  <h1>Authentication</h1>
);

export default Signx;
